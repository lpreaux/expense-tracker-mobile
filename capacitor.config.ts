import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'fr.lucaspreaux.expense-tracker-mobile',
  appName: 'expense-tracker-mobile',
  webDir: 'www'
};

export default config;
